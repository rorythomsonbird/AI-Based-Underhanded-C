
#include <stdio.h>

int main() {
    printf("Welcome to the GPT API!\n");

    // Prompt Engineering
    char prompt[100];
    printf("Please enter your prompt: ");
    fgets(prompt, sizeof(prompt), stdin);

    // Call GPT API
    // gpt_generate(prompt);

